// TODO: a fields table that shows all fields associated with a farm,
// with collapsable rows to show details for each field, including active planted crop,
// and information about the crop (GDD, life stage, etc.), soil samples, pest samples, etc.
// should connect to a map view of the whole farm (FarmMap.jsx), so when a field is selected 
// on the map, it expands the corresponding field row