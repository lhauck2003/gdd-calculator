// TODO: implement table to show all crops
// with ability to edit/add/delete crops, filter/sort by various attributes 
// (name, state, type, etc.), and view details for each crop (GDD target, growth stages, etc.)
// use Mateerial-UI for styling, specifically the DataGrid, TablePagination, 
// TableSortLabel, StickyHeader, ColumnGrouping, etc. components, with collapsable rows for 
// viewing growth stages and GDD targets for each stage, with added descriptions
export default function CropsTable({ crops }) {
    return (<></>);
}