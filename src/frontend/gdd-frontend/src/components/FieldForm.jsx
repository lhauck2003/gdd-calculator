// TODO: a form to create a new field on a farm, with the ability to input field name,
// outline the field area on a map (Leaflet), and save to the backend,
// then redirect to the Farms page (Farms.jsx) to view the new field and its farm's details,
// should be able to further edit field details from the fields page (Fields.jsx)
// use Material-UI components for styling, such as TextField, Button, etc.
// make sure to validate input data and handle errors appropriately, 
// with user-friendly messages and prompts
// then send data to backend API for storage and retrieval, 
// and update the frontend state accordingly to reflect changes in the UI
export default function FieldForm() {
    return (<></>);
}