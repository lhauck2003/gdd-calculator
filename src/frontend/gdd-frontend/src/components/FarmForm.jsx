// TODO: a form to create a new farm, with the ability to input farm name,
// outline the farm area on a map (Leaflet), and save to the backend,
// then redirect to the Farms page (Farms.jsx) to view the new farm and its details,
// and be able to edit farm details and fields from the farm page (Farms.jsx)
// use Material-UI components for styling, such as TextField, Button, etc.
// make sure to validate input data and handle errors appropriately, 
// with user-friendly messages and prompts
// then send data to backend API for storage and retrieval, 
// and update the frontend state accordingly to reflect changes in the UI
export default function FarmForm() {
    return (<></>);
}