// TODO: implement form for adding/editing soil samples for a specific field, with ability to input sample date, location, and various soil properties (pH, nutrient levels, etc.) and view historical soil sample data for the field
// should also include a map to select the location of the soil sample within the field, and a table to view past samples with sorting/filtering options
// use Material-UI components for styling, such as TextField, Button, DataGrid, etc.
// make sure to validate input data and handle errors appropriately, with user-friendly messages and prompts
// then send data to backend API for storage and retrieval, and update the frontend state accordingly to reflect changes in the UI
export default function SoilSampleForm({ fieldId }) {
    return (<></>);
}