// TODO: implement form for adding/editing pest samples for a specific field, with ability to input sample date, location, and various pest properties (type, severity, etc.) and view historical pest sample data for the field
// should also include a map to select the location of the pest sample within the field, and a table to view past samples with sorting/filtering options
// use Material-UI components for styling, such as TextField, Button, DataGrid, etc.
// make sure to validate input data and handle errors appropriately, with user-friendly messages and prompts
// then send data to backend API for storage and retrieval, and update the frontend state accordingly to reflect changes in the UI
export default function PestSampleForm({ fieldId }) {
    return (<></>);
}